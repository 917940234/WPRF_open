exp:
  type: transunet_wprf
  name: octa500_3mm_transunet_wprf
  output_dir: results/octa500_3mm/transunet_wprf
data:
  dataset_root: data/OCTA-500/OCTA_3mm/data
  image_size:
  - 320
  - 320
  cache_dir: data/_cache/octa500_3mm
runtime:
  device: cuda
  limit_cpu_threads: true
  cpu_threads: 1
  cv2_threads: 0
training:
  num_epochs: 300
  batch_size: 35
  num_workers: 12
  persistent_workers: true
  prefetch_factor: 4
  learning_rate: 0.0001
  weight_decay: 0.01
  seed: 42
  grad_clip_norm: 1.0
  save_checkpoints: true
infer:
  tau_u: 0.5
  tau_link: 0.5
visualization:
  num_images: 3
evaluation:
  split: test
  max_images: 3
  max_instances_per_image: 512
model:
  backbone_name: resnet18
  pretrained: true
  vit_embed_dim: 256
  vit_num_layers: 4
  vit_num_heads: 8
  decoder_channels: 64
